  <div class = "row">
    <%#= render 'books/error', model: @book %>
   <div class = "col-md-3">
    <%#= render 'users/user', user: @user %>
    
    <% form_with url: search_path,local, false, method: :image  do |f| %>
    <%= f.text_field: word,place holden, "検索したい内容を記入してください", class: form_box %>
    <%= f.submit "検索" btn %>
    <% end ％%>
    <h2>New image</h2>
      <%= render 'imagess/image', image: @image %>
   </div>
   
   <div class = "col-md-8 offset-md-1">
      <h2>image</h2>
      <table class = "table table-hover table-inverse">
          <thead>
          
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </thead>
          <tbody>
            <% @imsge.each do |image| %>
            <tr>
               <td></td>
               <td><%= link_to  user_path(image.user) do %>
               <td><%= image_tag @user.get_profile_image(100, 100) %></td>
               <% end %>
               </td>
               <td><%= link_to image.title, imagek_path(image.id) %></td>
              <td><%= image.body %></td>
            </tr>
            <% end %>
          </tbody>
      </table>
   </div>
  </div>